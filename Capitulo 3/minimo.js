function min(x, y) {
  return Math.min(x, y);
}
console.log(min(2, 1));

/* Valores de retorno
console.log(Math.min(2, 4)); //->2
console.log(Math.max(2, 4)); //->4

console.log(Math.max(2, 4) * 2); //->8*/
